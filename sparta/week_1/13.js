// 조건부 실행
// let x = 10;

// if (x > 0) {
//     console.log("x는 양수입니다.");
// }
// (논리곱 연산자 => &&) : 두 값이 모두 true일 때 true 반환 06.js 파일
// and 조건 (&&)
// (x > 0) && console.log("x는 양수입니다.")

// or조건(||)
// 삼항 연산자와 단축평가
let y; // y에는 undifined가 들어가있다.
let z = y || 20; // y가 존재하지 않는 경우에는 20이라는 값을 세팅 해달라.

console.log(z);